# Product original photos

Files in this folder are **byte-for-byte originals** as uploaded for the ZARVIA studio edit.

- Do not resize, recompress, or generate thumbnails that replace these sources.
- The site serves them via `next/image` with `unoptimized` so the browser gets the original file.
- Catalog mapping lives in `src/data/products.ts`.
